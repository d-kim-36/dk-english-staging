source "https://rubygems.org"

# Jekyll 4 + Minimal Mistakes via remote_theme
# Note: github-pages gem is not used here; deploy via GitHub Actions instead.
gem "jekyll", "~> 4.3"
gem "minimal-mistakes-jekyll"

group :jekyll_plugins do
  gem "jekyll-remote-theme"
  gem "jekyll-include-cache"
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
